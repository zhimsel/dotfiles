[user]
  email = zach@himsel.net
  name = Zach Himsel
[core]
  eol = lf
  autocrlf = input
  safecrlf = false
  editor = vim
  excludesfile = ~/.gitignore_global
[merge]
  tool = vimdiff
[push]
  default = tracking
[alias]
  unstage = reset HEAD --
  last = log -1 HEAD
  s = status
  f = fetch
  c = commit
  a = add
  ck = checkout
  co = checkout
  br = branch
  undo-last-commit = reset --soft HEAD~1
  sf = submodule foreach 'git checkout master && echo "" && git pull'
  sub = submodule
  branchdiff = "!sh -c \"git diff $(git merge-base master HEAD) HEAD\""
  l = log --graph --decorate --pretty=medium --all
  ll = log --graph --decorate --pretty=oneline --abbrev-commit --all
[pager]
  last = cat
