[user]
  email = zach@himsel.net
  name = Zach Himsel
[core]
  eol = lf
  autocrlf = input
  safecrlf = false
  editor = vim
  excludesfile = ~/.gitignore_global
[merge]
  tool = vimdiff
[push]
  default = tracking
[pager]
  last = cat
[alias]
  unstage = reset HEAD --
  last = log -1 HEAD
  s = status
  f = fetch
  c = commit
  a = add
  ck = checkout
  co = checkout
  br = branch
  undo-last-commit = reset --soft HEAD~1
  subf = submodule foreach 'git checkout master && echo "" && git pull'
  sub = submodule
  branchdiff = "!sh -c \"git diff $(git merge-base master HEAD) HEAD\""
  ll = log --graph --decorate --pretty=medium --all
  l = log --graph --decorate --abbrev-commit --all --pretty=format:\"%C(auto)%h %Cblue%an %Creset%C(auto)%s%d\"
  d = diff
  dc = diff --cached
